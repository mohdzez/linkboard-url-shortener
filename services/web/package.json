{
  "name": "linkboard-web",
  "version": "1.0.0",
  "private": true,
  "description": "Linkboard dashboard — server-rendered UI that talks to the API over the internal service link.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
